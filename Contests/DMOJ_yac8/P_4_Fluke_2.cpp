/***************************************************************************************************
 * author : Jie Chen (3rd Year CS)
 * school : Rochester Institute of Technology
 * created: 02.02.2024 09:38:10
****************************************************************************************************/
#include <bits/stdc++.h>

using namespace std;

#ifdef BROKEN_CODE
#include <bits/debug.h>
#else
#define dbg(...) 10082002
#define dbp(...) "Gimme Internship"
#endif

using i64 = long long;
using Pos = pair<int, int>;

Pos play_move(int r, int c) {
    printf("%d %d\n", r, c);
    fflush(stdout);
    scanf("%d%d", &r, &c);
    return make_pair(r, c);
}

void win(int n, int m) {
    
}

int main() {
    int n, m, t;
    scanf("%d%d%d", &n, &m, &t);

    for (; t--; ) {
        win(int n, int m);
    }
}

// ~ Just Jie